#include <stdio.h>

int main(void)
{
    // declare and initialize variables
    int x = 3;
    int y = 7;
    
    // print sum and product of variables
    printf("%d + %d = %d\n", x, y, x + y);
    printf("%d * %d = %d\n", x, y, x * y);
}